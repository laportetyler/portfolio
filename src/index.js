import React from 'react';
import ReactDOM from 'react-dom';
import PortfolioApp from "./PortoflioApp";

ReactDOM.render(
    <React.StrictMode>
        <PortfolioApp />
    </React.StrictMode>,
    document.getElementById('root')
);
